{
    "version": 2,
    "builds": [
        {
            "src": "main.py",
            "use": "@vercel/fastapi",
            "config": { "maxLambdaSize": "15mb" }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main.py"
        }
    ]
}
